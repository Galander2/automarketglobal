name: flutter_application_1_car_sales
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.0
  google_sign_in: ^7.2.0
  
  # UI
  cupertino_icons: ^1.0.8
  
  # Network & Images
  http: ^1.2.0
  image_picker: ^1.0.7
  
  # State Management
  provider: ^6.1.5+1
  
  # Localization (ИСПРАВЛЕННАЯ ВЕРСИЯ)
  intl: ^0.20.2
  
  # Storage
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
